# Relace API Key（必填）
# 取得方式：https://relace.run/ 註冊取得
RELACE_API_KEY=rlc-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 限制檔案存取範圍（強烈建議設定，防止路徑遍歷）
# 若未設定，預設為當前工作目錄
RELACE_BASE_DIR=/path/to/your/project

# 嚴格模式（選填，預設 false）
# 啟用時，RELACE_BASE_DIR 必須設定，否則拒絕啟動
# RELACE_STRICT_MODE=true

# Relace API Endpoint（選填）
# RELACE_ENDPOINT=https://instantapply.endpoint.relace.run/v1/code/apply

# Relace Model（選填，預設 relace-apply-3）
# RELACE_MODEL=relace-apply-3

# Log 檔案路徑（選填，預設 ~/.local/state/relace/relace_apply.log）
# RELACE_LOG_PATH=/var/log/relace/relace_apply.log

# Log 層級（選填，預設 INFO）
# 可選值：DEBUG, INFO, WARNING, ERROR
# RELACE_LOG_LEVEL=INFO

# API 請求 timeout 秒數（選填，預設 60）
# RELACE_TIMEOUT=60

# 最大重試次數（選填，預設 3）
# RELACE_MAX_RETRIES=3

# 重試基礎延遲秒數（選填，預設 1.0）
# RELACE_RETRY_BASE_DELAY=1.0
